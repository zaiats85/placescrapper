paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development

    development:
        adapter: mysql
        host: localhost
        name: slim
        user: root
        pass: zgoba-90
        port: 3306
        charset: utf8
