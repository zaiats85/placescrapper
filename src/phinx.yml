paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development

    development:
        adapter: mysql
        host: localhost
        name: exampleapp
        user: user
        pass: password
        port: 3306
        charset: utf8
